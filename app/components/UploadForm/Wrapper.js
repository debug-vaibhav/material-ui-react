import styled from 'styled-components';

const Wrapper = styled.div`
    & .file-app-input {
        width: 400px;
    }
    & .info-icon-spacing {
        margin-left: 10px
    }
`;

export default Wrapper;
